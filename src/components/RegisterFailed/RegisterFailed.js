import "./RegisterFailed.css";

const RegisterFailed = () => {
    return (
        <div className={"register-failed"}>
            The user already exists, try with a different user.
        </div>
    );
};

export default RegisterFailed;
